version: '3.9'

# Production Docker Compose for Synology NAS
# This configuration works with Synology's built-in reverse proxy
# No need for separate Nginx container - Synology handles HTTPS termination

services:
  # PostgreSQL Database
  hearth-db:
    image: postgres:16-alpine
    container_name: hearth-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-hearth_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-hearth_db}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - hearth-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-hearth_user} -d ${POSTGRES_DB:-hearth_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - hearth-network

  # MinIO Object Storage (S3-compatible)
  hearth-minio:
    image: minio/minio:latest
    container_name: hearth-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_BROWSER_REDIRECT_URL: ${MINIO_BROWSER_REDIRECT_URL:-http://localhost:9001}
    ports:
      - "${MINIO_API_PORT:-9000}:9000"      # MinIO API
      - "${MINIO_CONSOLE_PORT:-9001}:9001"  # MinIO Console (Web UI)
    volumes:
      - hearth-minio-data:/data
    networks:
      - hearth-network
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Next.js Application (Production)
  # Note: Port 3000 is exposed internally for Synology reverse proxy
  # Synology reverse proxy will handle HTTPS termination
  hearth-app:
    image: ${DOCKERHUB_USERNAME:-your-dockerhub-username}/hearth:${IMAGE_TAG:-latest}
    container_name: hearth-app
    restart: unless-stopped
    environment:
      # Database
      - DATABASE_URL=postgresql://${POSTGRES_USER:-hearth_user}:${POSTGRES_PASSWORD}@hearth-db:5432/${POSTGRES_DB:-hearth_db}?schema=public
      # Auth - IMPORTANT: Use HTTPS URL for production
      - NEXTAUTH_URL=${NEXTAUTH_URL:-https://hearth.blanchardsd.com}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
      - AUTH_TRUST_HOST=true
      # MinIO / S3 Configuration
      - S3_ENDPOINT=${S3_ENDPOINT:-http://hearth-minio:9000}
      - S3_ACCESS_KEY_ID=${MINIO_ROOT_USER:-minioadmin}
      - S3_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
      - S3_BUCKET_NAME=${S3_BUCKET_NAME:-hearth-uploads}
      - S3_REGION=${S3_REGION:-us-east-1}
      - S3_FORCE_PATH_STYLE=${S3_FORCE_PATH_STYLE:-true}
      # Weather API (OpenWeatherMap)
      - WEATHER_API_KEY=${WEATHER_API_KEY:-}
      # App
      - NODE_ENV=production
    # Port 3000 exposed for Synology reverse proxy to access
    # Can be restricted to localhost: "127.0.0.1:3000:3000" if desired
    ports:
      - "3000:3000"
    volumes:
      - hearth-uploads:/app/uploads  # Fallback for local storage if needed
    depends_on:
      hearth-db:
        condition: service_healthy
      hearth-minio:
        condition: service_healthy
    networks:
      - hearth-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  hearth-postgres-data:
    name: hearth-postgres-data
  hearth-minio-data:
    name: hearth-minio-data
  hearth-uploads:
    name: hearth-uploads

networks:
  hearth-network:
    name: hearth-network
    driver: bridge
