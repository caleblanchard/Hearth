# Production Environment Variables for Docker Compose
# Copy this file to .env.production and fill in your actual values

# ==========================================
# Docker Configuration
# ==========================================
DOCKERHUB_USERNAME=your-dockerhub-username
IMAGE_TAG=latest

# ==========================================
# Application
# ==========================================
APP_PORT=3000
NODE_ENV=production

# ==========================================
# NextAuth Configuration
# ==========================================
NEXTAUTH_URL=https://your-domain.com
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-min-32-characters
# Trust all hosts (set to true for internal networks, or configure specific hosts)
AUTH_TRUST_HOST=true

# ==========================================
# Database Configuration
# ==========================================
POSTGRES_USER=hearth_user
POSTGRES_PASSWORD=your-secure-postgres-password
POSTGRES_DB=hearth_db

# ==========================================
# MinIO Object Storage Configuration
# ==========================================
# MinIO Root Credentials (Admin access)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=your-secure-minio-password-min-8-chars

# MinIO Ports
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# MinIO Browser Redirect (for accessing the web console)
MINIO_BROWSER_REDIRECT_URL=http://localhost:9001

# ==========================================
# S3 Configuration (for app to connect to MinIO)
# ==========================================
# Internal endpoint (from within Docker network)
S3_ENDPOINT=http://hearth-minio:9000

# Use the MinIO credentials
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=your-secure-minio-password-min-8-chars

# Bucket name (will be created automatically by app)
S3_BUCKET_NAME=hearth-uploads

# Region (can be any value for MinIO)
S3_REGION=us-east-1

# Force path-style URLs (required for MinIO)
S3_FORCE_PATH_STYLE=true

# ==========================================
# VAPID Keys for Push Notifications
# ==========================================
# Generate with: node scripts/generate-vapid-keys.js
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key

# ==========================================
# Optional: External S3 Configuration
# ==========================================
# If you want to use AWS S3 or another S3-compatible service instead of MinIO,
# uncomment and configure these variables:
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_ACCESS_KEY_ID=your-aws-access-key
# S3_SECRET_ACCESS_KEY=your-aws-secret-key
# S3_BUCKET_NAME=your-s3-bucket-name
# S3_REGION=us-east-1
# S3_FORCE_PATH_STYLE=false
